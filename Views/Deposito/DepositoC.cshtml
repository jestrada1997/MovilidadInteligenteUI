@model MovilidadInteligenteUI.Models.Deposito

@{
    ViewData["Title"] = "Depositar";
}

<br />
<body>
    <br />
    <div class="container">
            <div class="card">
                <div class="card-body">
                    <h4>Hacer un deposito</h4>
                    <hr />

    <h4>Pago</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Crear">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @Html.HiddenFor(model => model.idDeposito)

                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                @Html.HiddenFor(model => model.idUsuario)
                @*<div class="form-group">
                    <label asp-for="idUsuario" class="control-label"></label>
                    <input asp-for="idUsuario" class="form-control" />
                    <span asp-validation-for="idUsuario" class="text-danger"></span>
                </div>*@
                <div class="form-group">
                    <label asp-for="monto" class="control-label"></label>
                    <input asp-for="monto" class="form-control" />
                    <span asp-validation-for="monto" class="text-danger"></span>
                </div>  
                <div class="form-group">
                    <label asp-for="estado" class="control-label"></label>
                    <input asp-for="estado" class="form-control" />
                    <span asp-validation-for="estado" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <a class="btn btn-primary" @Html.ActionLink("Depositar", "DepositoCliente", new { id = Model.monto,usuario = Model.idUsuario })</a>
                    <input type="submit" value="Depositar" class="btn btn-primary" />
                    <a asp-action="Perfil" class="btn btn-primary">Regresar</a>
                </div>
            </form>
        </div>
    </div>
                </div>
            </div>
        </div>
    <div>

    </div>
</body>
    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
